name: Deploy Backend (Railway)
# Railway auto-deploys from GitHub on every push to main.
# This file is kept as a placeholder — no manual deploy needed.
on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "✅ Railway deploys automatically on push to main. No action needed."
